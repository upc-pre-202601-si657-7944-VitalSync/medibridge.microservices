# Docker environment

Para que `payments-service` funcione correctamente, crea un archivo `.env` dentro de esta carpeta con las siguientes variables:

```env
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
```
