server:
  port: ${PORT:${SERVER_PORT:8080}}

spring:
  application:
    name: api-gateway
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: iam-authentication
              uri: ${IAM_SERVICE_URL:http://localhost:8081}
              predicates:
                - Path=/api/v1/authentication/**
              filters:
                - AddRequestHeader=X-Internal-Token, ${INTERNAL_SERVICE_TOKEN:local-internal-token}
            - id: iam-users
              uri: ${IAM_SERVICE_URL:http://localhost:8081}
              predicates:
                - Path=/api/v1/users/**
              filters:
                - AddRequestHeader=X-Internal-Token, ${INTERNAL_SERVICE_TOKEN:local-internal-token}
            - id: iam-roles
              uri: ${IAM_SERVICE_URL:http://localhost:8081}
              predicates:
                - Path=/api/v1/roles/**
              filters:
                - AddRequestHeader=X-Internal-Token, ${INTERNAL_SERVICE_TOKEN:local-internal-token}
            - id: iam-jwks
              uri: ${IAM_SERVICE_URL:http://localhost:8081}
              predicates:
                - Path=/api/v1/jwks/**
              filters:
                - AddRequestHeader=X-Internal-Token, ${INTERNAL_SERVICE_TOKEN:local-internal-token}
            - id: profiles-api
              uri: ${PROFILES_SERVICE_URL:http://localhost:8082}
              predicates:
                - Path=/api/v1/profiles/**
              filters:
                - AddRequestHeader=X-Internal-Token, ${INTERNAL_SERVICE_TOKEN:local-internal-token}
            - id: iam-openapi-docs
              uri: ${IAM_SERVICE_URL:http://localhost:8081}
              predicates:
                - Path=/docs/iam/v3/api-docs
              filters:
                - AddRequestHeader=X-Internal-Token, ${INTERNAL_SERVICE_TOKEN:local-internal-token}
                - SetPath=/v3/api-docs
            - id: profiles-openapi-docs
              uri: ${PROFILES_SERVICE_URL:http://localhost:8082}
              predicates:
                - Path=/docs/profiles/v3/api-docs
              filters:
                - AddRequestHeader=X-Internal-Token, ${INTERNAL_SERVICE_TOKEN:local-internal-token}
                - SetPath=/v3/api-docs

gateway:
  cors:
    allowed-origins: ${GATEWAY_CORS_ALLOWED_ORIGINS:*}

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
    urls-primary-name: IAM Service
    urls:
      - name: IAM Service
        url: /docs/iam/v3/api-docs
      - name: Profiles Service
        url: /docs/profiles/v3/api-docs
